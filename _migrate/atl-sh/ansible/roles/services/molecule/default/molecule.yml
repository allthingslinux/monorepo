---
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml

driver:
  name: vagrant
  provider:
    name: libvirt

platforms:
  - name: instance
    box: debian/trixie64
    memory: 1024
    cpus: 1

provisioner:
  name: ansible

verifier:
  name: ansible
