# =============================================================================
# atl.tools — Root Compose
# =============================================================================
# Orchestrates all self-hosted services.
#
# Usage:
#   just up                  # Start all services
#   just up-service searxng  # Start a specific service
#   just logs privatebin     # Tail logs for a service
# =============================================================================

name: atl-tools

include:
  - apps/networks.yaml
  - apps/privatebin/compose.yaml
  - apps/cyberchef/compose.yaml
  - apps/convertx/compose.yaml
  - apps/searxng/compose.yaml
  - apps/it-tools/compose.yaml
  - apps/jsoncrack/compose.yaml
  - apps/stirling-pdf/compose.yaml
