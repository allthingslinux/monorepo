---
ntp_timezone: UTC
ntp_manage_config: true
ntp_servers:
  - 0.pool.ntp.org iburst
  - 1.pool.ntp.org iburst

common_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - wget
  - gnupg
  - unattended-upgrades
  - sudo
  - rsync
  - man-db
