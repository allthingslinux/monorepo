{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "type-check": {
      "dependsOn": ["transit", "typegen"]
    },
    "build": {
      "dependsOn": ["^build", "typegen"]
    }
  }
}